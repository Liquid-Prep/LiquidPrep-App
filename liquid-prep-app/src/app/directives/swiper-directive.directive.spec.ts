import { SwiperDirectiveDirective } from './swiper-directive.directive';

describe('SwiperDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new SwiperDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
